{
  "name": "apis",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "bs": "npm run build && npm run start",
    "dev": "cross-env nodemon --watch 'src/**/*.ts' --exec 'npx ts-node -r tsconfig-paths/register src/app.ts'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.7",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
